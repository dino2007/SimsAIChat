name: Malware Scan
on: [push]

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Virus Scan
        uses: dell/common-github-actions/malware-scanner@main
        with:
          directories: .
